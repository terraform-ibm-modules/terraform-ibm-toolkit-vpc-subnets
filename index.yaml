id: github.com/cloud-native-toolkit/terraform-ibm-vpc-subnets
name: ibm-vpc-subnets
type: terraform
description: Module to provision a collection of subnets for an existing VPC
tags:
  - network
  - subnets
  - vpc
versions:
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_name
        type: string
        description: >-
          The name of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: name
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: '[]'
      - name: tags
        type: list(string)
        default: []
        description: Tags that should be added to the instance
    version: v1.13.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_name
        type: string
        description: >-
          The name of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: name
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: '[]'
    version: v1.12.2
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_name
        type: string
        description: >-
          The name of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: name
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: []
    version: v1.12.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_name
        type: string
        description: >-
          The name of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: name
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: '[]'
    version: v1.12.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_name
        type: string
        description: >-
          The name of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: name
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: '[]'
      - name: enabled
        type: bool
        description: Flag to indicate that IBM VPC module should be enabled
        default: true
    version: v1.11.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
      - name: enabled
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: id
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: '[]'
      - name: enabled
        type: bool
        description: Flag to indicate that IBM VPC module should be enabled
        default: true
    version: v1.10.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
      - name: enabled
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: id
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        type: |-
          list(object({
               name=string,
               action=string,
               direction=string,
               source=string,
               destination=string,
               tcp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               udp=optional(object({
                 port_min=number,
                 port_max=number,
                 source_port_min=number,
                 source_port_max=number
               })),
               icmp=optional(object({
                 type=number,
                 code=optional(number)
               })),
             }))
        description: List of rules to set on the subnet access control list
        default: []
    version: v1.9.7
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 0.0.0'
        optional: true
    variables:
      - name: resource_group_id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: id
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        description: List of rules to set on the subnet access control list
        default: '[]'
    version: v1.9.6
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
        moduleRef:
          id: resource-group
          output: id
      - name: region
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
        scope: global
      - name: vpc_name
        type: string
        description: The name of the vpc instance
        moduleRef:
          id: vpc
          output: name
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: '0'
      - name: _count
        type: number
        description: The number of subnets that should be provisioned
        default: '3'
        scope: module
        important: true
      - name: label
        type: string
        description: Label for the subnets created
        default: default
        scope: module
      - name: gateways
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: '[]'
        moduleRef:
          id: gateways
          output: gateways
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: '256'
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
      - name: acl_rules
        description: List of rules to set on the subnet access control list
        default: '[]'
    version: v1.9.5
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: names
        description: The ids of the created subnets
      - name: subnets
        description: The subnets that were created
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: _count
        scope: module
        important: true
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.9.4
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: _count
        scope: module
        important: true
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.9.3
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: _count
        scope: module
        important: true
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.9.2
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: _count
        scope: module
        important: true
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.9.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    providers:
      - name: ibm
        source: ibm-cloud/ibm
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: _count
        scope: module
        important: true
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.9.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        important: true
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.8.2
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.8.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.8.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
      - name: vpc_name
        description: The name of the VPC where the subnets were provisioned
      - name: vpc_id
        description: The id of the VPC where the subnets were provisioned
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.7.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.6.2
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs: []
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.6.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.6.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
        type: string
        description: The id of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(object({cidr = string}))
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: list(object({}))
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.5.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
        type: string
        description: The id of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(object({cidr = string}))
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: acl_rules
        type: string
        description: List of rules to set on the subnet access control list
        default: []
        optional: true
    version: v1.5.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
        type: string
        description: The id of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(object({cidr = string}))
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.4.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
      - id: cos_bucket
        refs:
          - source: >-
              github.com/cloud-native-toolkit/terraform-ibm-object-storage-bucket
            version: '>= 0.0.1'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
        type: string
        description: The id of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: flow_log_cos_bucket_name
        moduleRef:
          id: cos_bucket
          output: bucket_name
        optional: true
        type: string
        description: Cloud Object Storage bucket id for flow logs (optional)
        default: ''
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(object({cidr = string}))
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: auth_id
        type: string
        description: >-
          The id of the authorization policy that allows the Flow Log to access
          the Object Storage bucket. This is optional and provided to sequence
          the authorization before the flow log creation.
        default: ''
        optional: true
    version: v1.3.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: names
        description: The ids of the created subnets
      - name: output[*].name }
        description: output[*].name }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
      - id: cos_bucket
        refs:
          - source: >-
              github.com/cloud-native-toolkit/terraform-ibm-object-storage-bucket
            version: '>= 0.0.1'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
        type: string
        description: The id of the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: flow_log_cos_bucket_name
        moduleRef:
          id: cos_bucket
          output: bucket_name
        optional: true
        type: string
        description: Cloud Object Storage bucket id for flow logs (optional)
        default: ''
      - name: zone_offset
        type: number
        description: >-
          The offset for the zone where the subnet should be created. The
          default offset is 0 which means the first subnet should be created in
          zone xxx-1
        default: 0
        optional: true
      - name: ipv4_cidr_blocks
        type: list(object({cidr = string}))
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: auth_id
        type: string
        description: >-
          The id of the authorization policy that allows the Flow Log to access
          the Object Storage bucket. This is optional and provided to sequence
          the authorization before the flow log creation.
        default: ''
        optional: true
    version: v1.3.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: ids
        description: The ids of the created subnets
      - name: output[*].id }
        description: output[*].id }
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
      - name: acl_id
        description: The id of the network acl for the subnets
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
      - id: cos_bucket
        refs:
          - source: >-
              github.com/cloud-native-toolkit/terraform-ibm-object-storage-bucket
            version: '>= 0.0.1'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: flow_log_cos_bucket_name
        moduleRef:
          id: cos_bucket
          output: bucket_name
        optional: true
        type: string
        description: Cloud Object Storage bucket id for flow logs (optional)
        default: ''
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.2.6
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
      - name: flow_log_cos_bucket_name
        type: string
        description: Cloud Object Storage bucket id for flow logs (optional)
        default: ''
        optional: true
    version: v1.2.5
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.2.4
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.2.3
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
      - name: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
        description: 'output: {id = subnet.id, zone = subnet.zone, label = var.label} ] }'
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.2.2
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ['10.10.10.0/24']). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.2.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ["10.10.10.0/24"]). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
      - name: provision
        type: bool
        description: >-
          Flag indicating that the subnet should be provisioned. If 'false' then
          the subnet will be looked up.
        default: true
        optional: true
    version: v1.2.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ["10.10.10.0/24"]). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
    version: v1.1.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
      - name: ipv4_cidr_blocks
        type: list(string)
        description: >-
          List of ipv4 cidr blocks for the subnets that will be created (e.g.
          ["10.10.10.0/24"]). If you are providing cidr blocks then a value must
          be provided for each of the subnets. If you don't provide cidr blocks
          for each of the subnets then values will be generated using the
          {ipv4_address_count} value.
        default: []
        optional: true
      - name: ipv4_address_count
        type: number
        description: >-
          The size of the ipv4 cidr block that should be allocated to the
          subnet. If {ipv4_cidr_blocks} are provided then this value is ignored.
        default: 256
        optional: true
    version: v1.1.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
        description: >-
          The id of the IBM Cloud resource group where the VPC has been
          provisioned.
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
        description: The name of the vpc instance
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
        description: The id of the network acl for the vpc instance
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: list(object({id = string, zone = string}))
        description: List of gateway ids and zones
        default: []
      - name: region
        scope: global
        type: string
        description: The IBM Cloud region where the cluster will be/has been installed.
      - name: ibmcloud_api_key
        scope: global
        type: string
        description: The IBM Cloud api token
      - name: _count
        scope: module
        type: number
        description: The number of subnets that should be provisioned
        default: 3
        optional: true
      - name: label
        scope: module
        type: string
        description: Label for the subnets created
        default: default
        optional: true
    version: v1.0.3
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: string
      - name: region
        scope: global
        type: string
      - name: ibmcloud_api_key
        scope: global
        type: string
      - name: _count
        scope: module
        type: string
      - name: label
        scope: module
        type: string
    version: v1.0.2
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
      - name: vpc_name
        moduleRef:
          id: vpc
          output: name
        type: string
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
      - name: gateways
        moduleRef:
          id: gateways
          output: gateways
        optional: true
        type: string
      - name: region
        scope: global
        type: string
      - name: ibmcloud_api_key
        scope: global
        type: string
      - name: _count
        scope: module
        type: string
      - name: label
        scope: module
        type: string
    version: v1.0.1
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms: []
    dependencies:
      - id: resource-group
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-resource-group
            version: '>= 1.0.0'
      - id: vpc
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc
            version: '>= 1.0.0'
      - id: gateways
        refs:
          - source: github.com/cloud-native-toolkit/terraform-ibm-vpc-gateways
            version: '>= 1.0.0'
        optional: true
    variables:
      - name: resource_group_id
        moduleRef:
          id: resource-group
          output: id
        type: string
      - name: vpc_id
        moduleRef:
          id: vpc
          output: id
      - name: acl_id
        moduleRef:
          id: vpc
          output: acl_id
        type: string
      - name: gateway_ids
        moduleRef:
          id: gateways
          output: ids
        optional: true
      - name: region
        scope: global
        type: string
      - name: ibmcloud_api_key
        scope: global
        type: string
      - name: _count
        scope: module
        type: string
      - name: label
        scope: module
        type: string
      - name: vpc_name
        type: string
      - name: gateways
        type: string
    version: v1.0.0
    outputs:
      - name: count
        description: The number of subnets created
      - name: subnets
        description: The subnets that were created
  - platforms:
      - kubernetes
      - ocp3
      - ocp4
    dependencies: []
    variables: []
    version: v0.0.0
interfaces:
  - github.com/cloud-native-toolkit/automation-modules#ibm-vpc-resource
registryId: cloud-native-toolkit/vpc-subnets/ibm
